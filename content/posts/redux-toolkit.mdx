---
title: How to Use Redux Toolkit in a Next.js App ?
summary: Redux Toolkit is the official way to write Redux logic. It helps manage global state in your app.
image: /images/posts/redux-toolkit.jpg
author: 'Esrafil'
publishedAt: '2026-07-13'
---

# What is Redux Toolkit?

## Explanation
Redux Toolkit is the official way to write Redux logic. It helps manage global state in your app.

---

## How to Install
Run this command in your terminal:

```
npm install @reduxjs/toolkit react-redux
```

---

## Step 1: Create a Store
Create **store/store.ts**:
```
import { configureStore } from '@reduxjs/toolkit';
export const store = configureStore({
  reducer: {},
});
export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```
---
### Step 2: Provide Store to App
In **app/layout.tsx**:
```
import { Provider } from 'react-redux';
import { store } from '@/store/store';
export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <Provider store={store}>
          {children}
        </Provider>
      </body>
    </html>
  );
}
```
---

### Step 3: Create a Slice (Example)
Create **store/features/productSlice.ts**:
```
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';
import api from '@/lib/axios';
// Async action to fetch products
export const fetchProducts = createAsyncThunk(
  'products/fetch',
  async () => {
    const response = await api.get('/products');
    return response.data.products;
  }
);
const productSlice = createSlice({
  name: 'products',
  initialState: {
    items: [],
    loading: false,
    error: null,
  },
  reducers: {
    // Sync actions here
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchProducts.pending, (state) => {
        state.loading = true;
      })
      .addCase(fetchProducts.fulfilled, (state, action) => {
        state.loading = false;
        state.items = action.payload;
      })
      .addCase(fetchProducts.rejected, (state, action) => {
        state.loading = false;
        state.error = action.error.message;
      });
  },
});
export default productSlice.reducer;
```
---

### Step 4: Add Slice to Store
Update **store/store.ts**:
```
import productReducer from './features/productSlice';

export const store = configureStore({
  reducer: {
    products: productReducer,
  },
});
```

---

### Step 5: Use in Component
```
import { useDispatch, useSelector } from 'react-redux';
import { fetchProducts } from '@/store/features/productSlice';
import { useEffect } from 'react';

export default function ProductList() {
  const dispatch = useDispatch();
  const { items, loading } = useSelector((state) => state.products);

  useEffect(() => {
    dispatch(fetchProducts());
  }, []);

  if (loading) return <div>Loading...</div>;

  return (
    <div>
      {items.map((product) => (
        <div key={product.id}>{product.title}</div>
      ))}
    </div>
  );
}
```